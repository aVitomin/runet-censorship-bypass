interface:
  display_name: "Verification Before Completion"
  short_description: "Verify completion claims with fresh evidence"
  default_prompt: "Use $verification-before-completion to verify the current work before reporting its status."

policy:
  allow_implicit_invocation: false
